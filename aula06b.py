# Digitando algo e mostrando se é um número ou não, se for Número vai retornar True, se não retorna False
n = input('Digite algo:')
print(n.isnumeric())

# Digitando algo e mostrando se é letra ou não, se for letra vai retornar True, se não retorna False
# n = input('Digite algo:')
# print(n.isalpha())

# Verificando se é alfanumérico ou não, se for retorna True, se não retorna False
# n = input('Digite algo:')
# print(n.isalnum())

# Verificando se a palavra toda é com letra maiúscula ou não, se estiver toda maiúscula vai retonar True, se não vai retorna False
# n = input('Digite algo:')
# print(n.isupper())
